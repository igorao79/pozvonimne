name: Build and Release

on:
  push:
    branches:
      - master
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Update version
        run: |
          # Get run number for version
          RUN_NUM=${{ github.run_number }}
          VERSION="0.1.${RUN_NUM}"
          TAG="v${VERSION}"
          echo "New version: $VERSION"
          echo "New tag: $TAG"

          # Update package.json version
          sed -i "s/\"version\": \"[^\"]*\"/\"version\": \"$VERSION\"/" package.json

          # Show updated version
          grep '"version"' package.json

      - name: Create Draft Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v0.1.${{ github.run_number }}
          name: Release 0.1.${{ github.run_number }}
          body: |
            Автоматический релиз #${{ github.run_number }}

            ## Скачивание
            - Linux AppImage: pozvonimne-*.AppImage

            ## Особенности
            - Автоматическая сборка при пуше в основную ветку
            - Поддержка Linux (Ubuntu)
          draft: true
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN || github.token }}

      - name: Build Next.js app
        run: npm run build

      - name: Build Electron app
        run: npm run build:electron:app
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false
